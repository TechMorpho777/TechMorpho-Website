# ═══════════════════════════════════════════════════════════════
#  CLEAN URLs - Remove .html extension
#  TechMorpho IT Solutions
# ═══════════════════════════════════════════════════════════════

# Enable URL Rewriting
RewriteEngine On
RewriteBase /

# Remove .html extension from URLs
# If the requested file doesn't exist as a directory
RewriteCond %{REQUEST_FILENAME} !-d
# And the requested file doesn't exist as a file
RewriteCond %{REQUEST_FILENAME}\.html -f
# Then rewrite the URL to add .html
RewriteRule ^([^/]+)$ $1.html [L]

# Redirect from .html to clean URL (forces clean URLs)
RewriteCond %{THE_REQUEST} ^GET\ /[^?\s]+\.html
RewriteRule (.*)\.html$ /$1 [R=301,L]

# Handle index.html - make homepage just /
RewriteCond %{THE_REQUEST} ^GET\ /index\.html
RewriteRule ^index\.html$ / [R=301,L]

# Remove trailing slash (optional, for consistency)
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_URI} (.+)/$
RewriteRule ^(.+)/$ /$1 [R=301,L]

# ═══════════════════════════════════════════════════════════════
#  SECURITY & PERFORMANCE
# ═══════════════════════════════════════════════════════════════

# Prevent directory browsing
Options -Indexes

# Set default charset
AddDefaultCharset UTF-8

# Enable GZIP compression
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# Browser Caching
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType image/x-icon "access plus 1 year"
</IfModule>

